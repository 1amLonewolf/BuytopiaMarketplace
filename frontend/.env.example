# Optional: full backend origin (no trailing slash). If unset, production builds use the
# default in src/utils/api.jsx; development uses the Vite proxy in vite.config.js.
# Example: VITE_API_URL=https://your-service.up.railway.app
# VITE_API_URL=
